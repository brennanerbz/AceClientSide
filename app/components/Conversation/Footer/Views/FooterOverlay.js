import React, { Component, PropTypes } from 'react';
import classnames from 'classnames';

export default class FooterOverlay extends Component {
	static propTypes = {
	}

	render() {
		return(
			<div id="footer_overlay">
			</div>
		);
	}
}